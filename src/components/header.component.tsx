import { HeaderContainer, Home } from "./styles/header.styled";

const Header = () => {
  return (
    <HeaderContainer>
      <Home to="/">Blog</Home>
    </HeaderContainer>
  );
};

export default Header;
